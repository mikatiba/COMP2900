arreglo = [4, 8, 10, 16, 32, 48]

suma = 0 #se inicializa en 0 para almacenar la suma total

for numero in arreglo: #por cada número en el arreglo se va sumando
    suma += numero

print(f"La suma total de los valores en el arreglo es {suma}")